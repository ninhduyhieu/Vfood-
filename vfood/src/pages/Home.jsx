import React from "react";
import Header from '../components/Header'

const Home = () => {
    return(
        <main className={'w-full flex flex-col'}>
            <Header/>
        </main>
    )
}

export default Home